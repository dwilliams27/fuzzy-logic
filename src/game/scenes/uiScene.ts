import { BaseScene } from "./baseScene";

export class UIScene extends BaseScene {
  constructor() {
    super({ key: 'UIScene' });
  }

  preload() {
    // Load stuff
    
  }

  create() {
    // Other stuff
  }
}